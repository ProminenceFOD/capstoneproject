apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup-task
spec:
  steps:
    - name: cleanup
      image: alpine:3.18
      script: |
        #!/bin/sh
        echo "Cleaning up temporary resources..."
        # Add cleanup commands here (e.g., delete PVCs, temp files)
---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: test-task
spec:
  steps:
    - name: run-tests
      image: node:20-alpine
      script: |
        #!/bin/sh
        echo "Running unit tests with Jest..."
        npm ci
        npx jest --ci
